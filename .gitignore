CLAUDE.md
/target
**/target
.idea/
# cross-build artifact (mingw-built libraw_r DLL), regenerated per-machine
/packaging/windows/vendor/
# mdBook build output (docs-site/src is the tracked source)
/docs-site/book/
