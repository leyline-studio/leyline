# Architecture Decision Records

Chaque décision structurante est consignée ici, selon le même plan : **contexte**, **décision**, **conséquences**, **alternatives écartées**. C'est la dernière rubrique qui fait la valeur d'un ADR — elle dit ce qui a été envisagé puis rejeté, et pourquoi, ce qu'aucune lecture du code ne permet de reconstituer.

## Comment lire ce dossier

Il n'est pas fait pour être lu d'un bout à l'autre. Trois usages :

* **Comprendre une brique externe** — 0001 à 0005 (Rust, Slint, SQLite, LibRaw, Lensfun/LittleCMS).
* **Comprendre le modèle de données** — 0007 (modèle inspiré de Git), 0008 (la version comme unité), 0010 (chemins relatifs).
* **Comprendre le rendu** — 0012 (parallélisme), 0028 puis **0042** et **0043** (versionnage du rendu), et les ADR de fonctionnalité pixel 0013, 0016–0018, 0029–0035.

## Règle d'édition

**Avant la première publication publique, un ADR reste librement modifiable** : un ADR qui ne correspond plus à l'implémentation est corrigé sur place, et non remplacé. Cette souplesse s'arrête le jour de l'ouverture au monde — les décisions acceptées gèlent alors, et sont remplacées par de nouveaux ADR qui les référencent, jamais éditées.

Ce qui ne se relâche d'aucun côté de cette ligne : **décider et documenter avant de coder**.

### Les chemins que citent les ADR d'avant 0042

Les ADR antérieurs à [0042](0042-versioned-stage-pipeline.md) nomment des modules `processN.rs` — `process1.rs` à `process11.rs` — qui **n'existent plus**. Le pipeline a été réorganisé en étages versionnés ([0042](0042-versioned-stage-pipeline.md)) puis son historique d'avant publication effondré ([0043](0043-collapse-prerelease-render-history.md)) : un opérateur qui vivait dans une copie complète du pipeline vit désormais dans `crates/leyline-engine/src/stages/<opérateur>/vN.rs`.

Ces citations ne sont **pas** corrigées, et c'est délibéré : elles décrivent un acte à sa date — « `process6.rs`, copie intégrale de `process5.rs` » raconte comment le module a été créé, et le réécrire en un chemin d'aujourd'hui rendrait la phrase fausse tout en la rendant cliquable. La règle d'édition ci-dessus vise ce qu'un ADR *affirme du présent*, pas ce qu'il raconte du passé.

Pour retrouver le code d'un tel ADR : l'opérateur qu'il décrit, sous `stages/`, à la version qu'il a créée.

Un ADR remplacé n'est jamais supprimé : il reste lisible, avec sa raison d'époque. [ADR 0028](0028-process-version-per-feature.md) en est l'exemple — son raisonnement était correct pour les données dont il disposait, et il avait lui-même prévu sa réouverture.

| ADR | Décision |
|---|---|
| [0001](0001-rust.md) | Rust comme langage unique |
| [0002](0002-slint.md) | Slint pour l'interface graphique |
| [0003](0003-sqlite.md) | SQLite comme unique base du catalogue |
| [0004](0004-libraw.md) | LibRaw (branche LGPL) pour le décodage RAW |
| [0005](0005-lensfun-littlecms.md) | Lensfun et LittleCMS |
| [0006](0006-blake3.md) | BLAKE3 pour les checksums |
| [0007](0007-git-develop-model.md) | Modèle de développement inspiré de Git |
| [0008](0008-version-as-library-unit.md) | La version comme unité de bibliothèque |
| [0009](0009-gpl3-cla-dual-license.md) | GPL-3.0 + CLA, modèle double licence |
| [0010](0010-relative-paths.md) | Bibliothèque autonome, chemins relatifs |
| [0011](0011-engine-api-model.md) | API moteur : lib Rust, sync/async, événements |
| [0012](0012-rayon-data-parallelism.md) | Parallélisme de données Rayon, rendu bit-pour-bit |
| [0013](0013-process-2-lut-transfer.md) | Process 2 : fonctions de transfert sRGB par table |
| [0014](0014-develop-presets.md) | Presets de développement : catégories partielles, application par révision normale |
| [0015](0015-color-management-srgb.md) | Gestion des couleurs V1 : pipeline et export figés en sRGB |
| [0016](0016-process-3-lens-correction.md) | Process 3 : correction géométrique d'objectif (Lensfun) |
| [0017](0017-process-4-vignetting.md) | Process 4 : correction du vignettage (Lensfun) |
| [0018](0018-process-5-tca.md) | Process 5 : aberration chromatique transversale (Lensfun) |
| [0019](0019-distribution-i18n.md) | Distribution : installateur par plateforme et internationalisation FR/EN |
| [0020](0020-menu-bar.md) | Barre de menu comme surface de commandes |
| [0021](0021-context-menus.md) | Menus contextuels (clic droit) |
| [0022](0022-default-library-fallback.md) | Bibliothèque par défaut au premier lancement |
| [0023](0023-catalog-lock-narrowing-preview.md) | Ne pas tenir le verrou catalogue pendant un rendu de preview |
| [0024](0024-catalog-lock-narrowing-export.md) | Ne pas tenir le verrou catalogue pendant un rendu d'export |
| [0025](0025-unified-export-request.md) | Une seule requête d'export : `ExportRequest`/`ExportRecipe` |
| [0026](0026-mask-spot-coordinate-referential.md) | Référentiel de coordonnées des masques et corrections locales : celui de `crop` |
| [0027](0027-color-management-beyond-srgb.md) | Gestion des couleurs au-delà de sRGB : profil de sortie découplé du rendu |
| [0028](0028-process-version-per-feature.md) | ~~Une process version par fonctionnalité pixel : duplication par module~~ — **remplacé par [0042](0042-versioned-stage-pipeline.md)** |
| [0029](0029-process-6-local-adjustments.md) | Process 6 : réglages locaux masqués (brosse, radial, gradient), stockés dans `settings_json`, pilotés par `Param::LocalAdjustment` |
| [0030](0030-tone-curve.md) | Courbe tonale : courbe par points seule, spline cubique monotone, appliquée en luminance via LUT |
| [0031](0031-hsl-color-grading.md) | Mélangeur TSL et roues de color grading : HSL dérivé du RGB, zones tonales pondérées par luminance |
| [0032](0032-spot-removal-clone.md) | Suppression de tache : clonage seul (heal coupé de V2), copie bilinéaire déterministe, tôt dans le pipeline |
| [0033](0033-clarity-texture-dehaze.md) | Clarté/texture (contraste local unifié à deux rayons) et dehaze (dark channel prior en forme close), globaux en V2 |
| [0034](0034-softproofing-watermark-print.md) | Épreuvage écran (vue seule) et filigrane texte (à l'export) : deux surfaces de sortie, aucun process ; le module d'impression reste hors décision |
| [0035](0035-camera-profile-dcp.md) | Profil caméra DCP : nouvel étage colorimétrique en tête de pipeline, fichiers fournis par l'utilisateur, référencés par chemin relatif et checksummés (BLAKE3) |
| [0036](0036-print-module.md) | Module d'impression : « export avec dimension physique + profil de destination », une photo par page (planches coupées), hand-off OS laissé à Studio |
| [0037](0037-dcp-parsing-dependency.md) | Dépendance de parsing DCP : lecteur de tags maison minimal au-dessus du crate `tiff` déjà lié, lecture seule, dans `leyline-color` |
| [0038](0038-tethered-capture.md) | Capture tethering : import direct depuis l'appareil via USB (libgphoto2), une capture est un import comme un autre |
| [0039](0039-watched-folder-import.md) | Import automatique par dossier surveillé (`notify`), un fichier stabilisé à la fois, même cœur d'import que le tethering |
| [0040](0040-gps-map-view.md) | Vue carte GPS : tuiles MBTiles hors-ligne fournies par l'utilisateur, aucun appel réseau, GPS extrait des RAW via LibRaw |
| [0041](0041-interactive-preview-rendering.md) | Rendu interactif : preview développée à la résolution d'affichage (rayons pixel mis à l'échelle) et cache d'étages de pipeline ; export inchangé, aucune nouvelle process version |
| [0042](0042-versioned-stage-pipeline.md) | Pipeline composé d'étages versionnés : le gel porte sur l'opérateur et non sur la version entière ; remplace ADR 0028, §5 amendé par 0043 |
| [0043](0043-collapse-prerelease-render-history.md) | Effondrement de l'historique de rendu avant publication : une version par opérateur, la révision porte sa carte `stages`, `process` disparaît |
| [0044](0044-linear-wide-gamut-working-space.md) | Tampon de travail en lumière linéaire large gamut : Rec. 2020 D65 non borné, étages `input`/`output_rendering`, espace déclaré par version d'étage |
| [0045](0045-studio-ui-modularisation.md) | Modularisation de l'interface Studio : un `global` Slint par domaine, un fichier par panneau/dialogue, modules Rust `wiring/` en miroir ; frontière SDK inchangée |
| [0046](0046-edge-preserving-denoise.md) | Débruitage préservant les contours : ondelettes à trous et seuillage doux par échelle, `noise_luminance::v2` / `noise_color::v2` (premier `v2` réel d'ADR 0042) |
| [0047](0047-xmp-sidecar-read.md) | Lecture des sidecars XMP : une amorce à l'import et à la demande, jamais une synchronisation ; remplir sans écraser, `roxmltree` pour le parsing |
| [0048](0048-range-masks.md) | Masques par plage : raffinement de luminance et de teinte multipliant la couverture géométrique, `local_adjustments::v2` ; un réglage inexprimable par la version épinglée est refusé |
| [0049](0049-local-adjustments-clients.md) | Exposer les retouches locales aux clients : outils de tracé sur la preview dans Studio (« neutre = absent »), payload JSON du `LocalAdjustment` stocké dans la CLI |
| [0050](0050-highlight-reconstruction.md) | Reconstruction des hautes lumières : modes `clip`/`blend`/`rebuild` de LibRaw avant dématriçage, épinglés par `input::v2` ; refus de validation sur une révision en `input: 1` |
| [0051](0051-watermark-rasterization-and-soft-proof-surface.md) | Filigrane texte rastérisé par `ab_glyph` avec police DejaVu Sans embarquée, dessiné dans `encode` ; épreuvage écran = `Library::preview_soft_proofed` en mémoire, alerte de gamut par LittleCMS |
| [0052](0052-perspective-correction.md) | Correction de perspective : homographie à deux curseurs (vertical, horizontal), étage `perspective` au rang 205 entre rotation et recadrage ; boîte englobante, pas de recadrage automatique |
| [0053](0053-creative-lut.md) | LUT créative : fichiers `.cube` importés dans `Profiles/LUT/` et référencés avec checksum, appliqués sur l'axe d'affichage au rang 160, dosables ; interpolation trilinéaire |
| [0054](0054-first-run-and-basic-mode.md) | Prise en main : une bibliothèque vide qui explique les deux gestes d'entrée, et un mode Basique par défaut dans develop (six groupes, deux outils) que le mode Complet rouvre en un clic ; aucun réglage ni rendu touché |
| [0055](0055-library-navigation.md) | Navigation de la bibliothèque : sélecteur de module dans la barre de menus, arbre des dossiers en lecture seule à gauche, barre d'outils (taille de vignette, loupe sur `E`), filmstrip partagé et badges de cellule ; aucune écriture nouvelle au catalogue |
| [0056](0056-non-raw-exif-import.md) | Métadonnées EXIF des fichiers non-RAW à l'import (`kamadak-exif` dans le moteur, à côté du lecteur XMP) : LibRaw garde la préséance, lecture seule, meilleur effort, `OffsetTimeOriginal` renseigne `capture_offset_minutes` |
| [0057](0057-compare-and-survey.md) | Départager deux photos : vue Comparaison à zoom et déplacement liés en coordonnées normalisées (retenue à gauche, candidate à droite) et vue Mosaïque qui réduit la sélection ; aucune écriture, previews en cache |
| [0058](0058-preset-provenance-and-shelf.md) | Préréglages : panneau gauche dans develop, un niveau de dossiers et des favoris, contenu lisible et essayable au survol, et surtout **provenance** — la révision enregistre de quel préréglage et de quelle version elle vient, dans le catalogue et non dans `settings_json` |
| [0059](0059-bundled-world-basemap.md) | Fond de carte mondial embarqué : Natural Earth z0–5 en JPEG (9 Mo), ouvert sans extraction par `sqlite3_deserialize`, derrière une feature que seul Studio active ; un pack importé gagne toujours |
| [0060](0060-asset-removal.md) | Retirer une photo du catalogue (`remove`, fichier intact) et la supprimer du disque (`delete`, corbeille du système) : deux opérations distinctes, cascade déjà présente au schéma, `Event::AssetsRemoved` ; rend applicable le remède d'ADR 0043 §5 |
| [0061](0061-demosaic-algorithm.md) | Choix de l'algorithme de dématriçage : quatre valeurs nommées (`ahd` par défaut, `vng`, `dcb`, `dht`) écrites dans la révision, portées par `input::v3` ; AMaZE/LMMSE indisponibles (packs GPL retirés de LibRaw), et sans effet sur les previews `half_size` |
| [0062](0062-dcp-illuminant-interpolation.md) | Interpolation des illuminants de calibration DCP : mélange en mireds selon la température de scène, résolu au rendu et non au parsing, `camera_profile::v2` ; remplace une moyenne qui coûtait jusqu'à 11 niveaux sur 255 |
| [0063](0063-dcp-tables.md) | Appliquer les tables d'un profil DCP (HueSatMap, LookTable, ToneCurve) : ordre et espace ProPhoto de la spec, interpolation trilinéaire à teinte cyclique, hautes lumières préservées en traversant les tables, `camera_profile::v3` |
| [0064](0064-metadata-filters.md) | Filtres de prise de vue sur la grille : boîtier, objectif, ISO, ouverture, focale, vitesse ; aucune migration ni index (le schéma les portait déjà), listes de valeurs construites depuis la bibliothèque, clause boîtier partagée avec les collections dynamiques |
| [0065](0065-selective-import.md) | Import sélectif : `scan_import` décrit sans rien écrire (imagette embarquée du fichier, 256 px, doublons marqués par nom+taille), `import_files` prend une liste explicite, `import(dossier)` inchangé ; planche-contact à cocher dans Studio, `scan` et `--only` dans la CLI |
| [0066](0066-sensor-white-level.md) | Le niveau de blanc vient du capteur (`linear_max` du fichier), plus du contenu de la photo : LibRaw abaissait `maximum` jusqu'au pixel le plus clair de chaque image, faisant varier la luminosité de 19 % entre deux photos d'une même série ; `input::v4`, seule version d'`input` non identique à sa précédente à réglages neutres |
| [0067](0067-avif-encode-speed.md) | La vitesse d'encodage AVIF devient un champ d'`ExportSettings` (1–10, toujours sérialisé donc épinglé par le preset) et son défaut passe de 6 à 9 : mesuré sur le chemin d'export complet, 9 rend 28 % du temps et 53 % du CPU pour 1,3 % de poids, et le bon compromis dépend de la photo — ce qui est l'argument contre une constante ; l'image ne change pas (46–51 dB entre vitesses), seuls le temps et le poids |
| [0068](0068-concurrent-export-batch.md) | Un lot d'export traite plusieurs photos à la fois : le pipeline d'une seule photo n'utilisait que 289 % de 1 600 % sur seize threads, et B2 se trompait d'ordre de grandeur (10-15 % espérés, 2,8× obtenus). Degré 4 par défaut (2,81× pour 2,4 Go de pointe), réglable par `ExportRequest::concurrency` ; noms de sortie réservés d'avance en ordre de requête pour que deux versions d'un même asset se collisionnent de façon déterministe plutôt que de courir au même chemin ; threads ordinaires, jamais des tâches rayon |
| [0069](0069-closed-extension-boundary.md) | Une extension fermée s'attache par le SDK, elle ne duplique pas le dépôt : elle produit des **réglages**, jamais des pixels, donc aucun code fermé n'entre dans le chemin de rendu et §5.1 ne rencontre jamais la frontière de licence. Un clone est refusé parce que deux dépôts ne peuvent pas être tous deux propriétaires de `sharpen::v1`. Permission additionnelle GPLv3 §7 à consigner ; le système de clé n'est pas tranché ici |
| [0070](0070-stored-mask-coverage.md) | `Mask::Coverage` : un masque peut être une couverture **stockée** (PNG gris 16 bits, chemin relatif + BLAKE3, à sa propre résolution) et plus seulement une formule géométrique — échantillonné bilinéairement sur le même canevas normalisé que les quatre variantes existantes, donc rotation, plages et opacité s'y appliquent inchangées. `local_adjustments::v3` ; `v1`/`v2` **refusent** un masque stocké au lieu de l'ignorer. Moitié ouverte et gratuite d'[ADR 0069](0069-closed-extension-boundary.md) |
| [0071](0071-mask-overlay.md) | La couverture du masque sélectionné en surimpression rouge sur l'aperçu — dernier reliquat d'[ADR 0049](0049-local-adjustments-clients.md), et le seul moyen de voir un masque stocké, qui ne dessine aucune poignée. Une **vue**, pas un rendu : aucune version d'étage, §5.1 hors de cause. Rasterisée au rang 160 (donc plages comprises) puis poussée à travers `rotate`/`perspective`/`crop` pour tomber juste ; les opérateurs de pixels entre les deux sont sautés |
| [0072](0072-measured-noise-profile.md) | Le débruitage sait de quel capteur il vient : la variance mesurée par boîtier et par ISO (table darktable, GPL-3.0-or-later, **gelée avec la version d'étage**) remplace trois constantes universelles, et le seuil devient un seuil **par pixel** — `k · 6σ_l · √(a·x + b)`. `noise_luminance::v3` et `noise_color::v3` changent aussi de **rang** (5 et 6, en lumière linéaire) : un modèle mesuré sur les comptes du capteur ne veut plus rien dire après l'exposition et la courbe tonale. Boîtier inconnu = modèle par défaut, jamais un étage muet ; `render` prend un `SensorShot` de plus, rien n'entre dans la révision |
| [0073](0073-external-mask-detectors.md) | Les masques détectés : une **prise ouverte** (`leyline-detect`, ré-exporté par le SDK) et des détecteurs séparés. Un détecteur est un **exécutable** qui prend un PNG et rend un PNG gris 16 bits — il n'ouvre pas la bibliothèque, ne prend aucun verrou, ne lie pas le SDK, et la frontière de licence est franchie par un `execve`. Le client ouvert transforme l'image en réglage (`store_mask_coverage`, ADR 0070). Découverte par manifeste dans la configuration utilisateur ; aucun manifeste, aucun menu. Licence des **poids** éliminatoire — SegFormer/NVIDIA et RMBG-1.4 écartés (non commerciaux), U²-Net/BiRefNet/MMSeg retenus. Le moteur ne bouge pas d'une ligne |
| [0074](0074-live-preview-while-dragging.md) | Le rendu suit le curseur : `EditSlider` n'émettait que sur le relâchement, donc on développait à l'aveugle — alors qu'[ADR 0041](0041-interactive-preview-rendering.md) chiffrait partout « le coût d'un déplacement de curseur » et avait rendu le moteur rapide pour une interaction jamais câblée. Un second signal pendant le geste, qui rend par le cache d'étages (~14 ms) et **n'écrit rien** : pas de révision, pas d'aperçu caché, §5.1 hors de cause. Borné à un rendu / 40 ms, les déplacements arrivés trop tôt étant **ignorés** et non mis en file. Mesuré sur de vrais CR2 : **~59 ms par image**, identique d'un 10 Mpx à un 30 Mpx et identique en tête ou en fin de pipeline — le poste dominant n'est donc pas le pipeline mais la réduction du buffer à la taille d'affichage, refaite à chaque image ; la mettre en cache est la suite nommée. Les curseurs seuls : les autres gestes créent une entrée, ils n'ont pas de continuum à montrer |
| [0075](0075-preview-cache-retention.md) | Le cache d'aperçus garde une **fenêtre**, pas tout l'historique : rien ne le purgeait jamais, et cent retouches sur une photo laissaient ~70 Mo d'aperçus périmés — plus que le RAW. Sont conservés la tête de chaque copie virtuelle et les **trois** révisions les plus récentes ; au-delà on régénère à la demande (une seconde, au quatrième undo consécutif) plutôt que de précharger. L'éviction a lieu là où le cache grossit — après l'enregistrement d'un aperçu — donc sans balayage ni tâche de fond. Aucune révision supprimée, aucune migration, §5.1 hors de cause |
| [0076](0076-proxy-cache.md) | Le proxy d'affichage se met en cache : [ADR 0041](0041-interactive-preview-rendering.md) §1 décide de réduire le décodage avant de développer, sans rien dire du résultat — qui était rebâti à chaque image d'un glissement, sur la même photo et la même classe. C'est le poste dominant nommé par [ADR 0074](0074-live-preview-while-dragging.md) §3. `DecodeCache` devient un cache de **buffers source** (décodages *et* proxies), clé `(asset, DecodeParams, max_edge)` — rien d'autre n'entre dans le calcul, donc rien d'autre dans la clé. Plafond en **octets** (64 Mo) et non en entrées, les classes s'étalant sur 250× ; l'entrée la plus récente survit toujours. Un succès saute la réduction *et* le décodage. Mesuré : **55 → 15 ms** (10 Mpx) et **49 → 11 ms** (30 Mpx), soit ~70 images/s — c'est désormais la borne des 40 ms d'ADR 0074 qui limite, pas la machine. Aucun pixel, aucun étage, aucune persistance |
| [0077](0077-application-updates.md) | Se mettre à jour : dernier manque de la distribution d'[ADR 0019](0019-distribution-i18n.md). Manifeste JSON portant la **signature minisign de chaque paquet**, vérifiée sur les octets téléchargés avant installation — le manifeste lui-même n'est pas signé (`cargo-packager-updater`, clé privée hors dépôt et hors CI), publié en asset de release et interrogé à la redirection stable `releases/latest/download/latest.json` — **compilée en dur**, parce qu'une URL de mise à jour réglable est une surface de détournement. GitHub Releases plutôt qu'un serveur du projet : un binaire installé interroge son URL pendant des années, et un projet local-first n'opère pas un service dont chaque installation dépend. **Cadence de Firefox sans son silence** : question posée **une fois** (défaut « non »), puis une vérification par lancement plafonnée à 24 h, **aucun minuteur en session** — mais l'installation demande toujours un clic. Ce qui satisfait `vision.md` au sens fort (autorisé en connaissance de cause) plutôt qu'au sens littéral. Trouvaille en signalant : une **pastille** sur le menu Aide, possible seulement parce que la barre est écrite à la main (correction d'[ADR 0020](0020-menu-bar.md)) — un `MenuBar` natif ne se décore pas. Le silence complet est écarté sur un fait : `cargo-packager-updater` **ne sait pas différer** une installation, donc « à la Firefox » serait plus intrusif que Firefox. Le réglage vit dans le panneau Préférences, qui a désormais deux locataires (avec la langue d'ADR 0019) et sa propre ADR à écrire. Rien n'est transmis, hors ligne n'est pas une erreur, trouver une version ne l'installe pas. Et, indépendamment du réseau : `catalog.db` est **copié dans `Backups/` avant toute migration** — elles sont sans retour arrière et `Catalog::open` refuse un catalogue plus récent, donc une mise à jour à un clic rend « revenir en arrière » atteignable par accident ; échec de copie = échec d'ouverture. `VACUUM INTO` et non une copie de fichier : en WAL, des pages validées vivent hors de `catalog.db`, et une copie naïve perd en silence le travail le plus récent |
| [0078](0078-preferences-panel.md) | Préférences : une **règle d'admission** écrite avant les deux réglages qu'elle admet — un réglage y entre s'il porte sur l'installation, doit survivre à un relancement, et n'a pas d'endroit naturel là où il agit ; et **aucune préférence ne peut changer un pixel**, sinon `pipeline.md` §5.1 deviendrait faux sans qu'une ligne du pipeline ait bougé. Elle rend explicites les refus d'[ADR 0022](0022-default-library-fallback.md), [0075](0075-preview-cache-retention.md) et [0077](0077-application-updates.md), et rejette d'emblée le mode Basique/Complet d'[ADR 0054](0054-first-run-and-basic-mode.md) (il a déjà un endroit). Dialogue modal dans l'overlay d'[ADR 0045](0045-studio-ui-modularisation.md), ni fenêtre (Studio n'en a qu'une, son changement de bibliothèque relance le processus) ni vue ; ni OK ni Annuler. La **langue change à chaud** — constaté, pas conçu : `translate_from_bundle` lit une propriété et aucun `slint::tr!()` n'existe côté Rust ; en contrepartie le nom natif des langues coûte une ligne de Rust, et la promesse d'[ADR 0019](0019-distribution-i18n.md) est corrigée d'autant. Le consentement d'[ADR 0077](0077-application-updates.md) est posé au **second** lancement (le premier appartient à [ADR 0054](0054-first-run-and-basic-mode.md)), et **toute façon de fermer le dialogue vaut « non »**, stocké, jamais reposé — ce qui empêche structurellement le harcèlement. `preferences.json` à côté de `recent_libraries.json`, écriture atomique, et **tous les chemins d'échec dégradent vers « hors ligne »** : un fichier perdu ne peut jamais activer une vérification réseau |
| [0079](0079-raw-jpeg-pairing.md) | Le RAW et son JPEG sont **une seule photo** : constat sur corpus réel — 39 prises de vue affichées en 78 — d'un trou qu'aucun ADR ni aucune exclusion ne couvrait. Une colonne `assets.companion_of` (migration v3), pas une table de piles : la relation est asymétrique et se lit à chaque grille. Critère en **conjonction** — même radical, même instant de capture, même boîtier, **à toute profondeur** de la bibliothèque, le corpus rangeant les RAW dans un sous-dossier ; chacun des trois pris seul appaire à tort (les rafales à 1/500 s partagent la seconde). Le maître est le RAW, une paire fait un RAW et un non-RAW, aucune chaîne. L'appairage a lieu à l'import **dans les deux sens**, le second étant le cas normal : l'énumération triée livre le JPEG avant le RAW. La grille montre les maîtres par **une clause**, donc comptage, filtres, recherche et collections suivent sans rien savoir des paires — c'est l'argument contre un regroupement fait à l'affichage, qui ferait mentir le catalogue à l'interface. Rien n'est caché : le compagnon se voit dans les détails, se supprime avec son maître (liste **étendue compagnon d'abord**, sans quoi la cascade efface sa ligne et laisse son fichier sur le disque) et se détache. La migration **n'appaire rien** — une passe explicite le fait, parce qu'une bibliothèque dont la moitié des vignettes disparaît au premier lancement est le pire moment pour découvrir une fonctionnalité. Option d'import et non préférence (règle d'admission d'[ADR 0078](0078-preferences-panel.md) §1). Aucun pixel, §5.1 hors de cause |
| [0080](0080-the-promise-and-its-boundary.md) | **La promesse, et sa frontière** : cinq garanties énoncées comme des propriétés de l'application installée (fichiers jamais modifiés, réglages lisibles sans nous, hors ligne sans compte ni vérification de licence, mêmes réglages mêmes pixels, rien ne sort sans un geste) — et, à côté d'elles, ce qui n'est **pas** promis, seul moyen qu'elles vaillent quelque chose. `specification.md` §4 énonçait trois exclusions comme des **principes** (« l'abonnement contredit la propriété »), ce qui aurait fait de tout service payant ultérieur le reniement d'un texte plutôt qu'un ajout ; elles redeviennent des décisions de périmètre. Les cinq conditions qu'un service optionnel devrait satisfaire sont écrites **maintenant**, sans revenu en jeu : l'application reste complète sans lui, le catalogue local reste l'original, tout s'exporte et survit à son arrêt, ce qui se vend est l'hébergement et jamais le droit d'exécuter, rien ne part sans geste explicite — même forme que la frontière d'[ADR 0069](0069-closed-extension-boundary.md) et que les conditions posées à l'IA locale. Débloque au passage le GPU, que `measured-findings.md` §B3 disait « interdit par §5.1 » : un backend appartient à la **version d'étage**, un portage GPU est une version neuve, et un repli CPU silencieux est refusé — deux backends rendant deux images d'une même révision sont précisément ce que §5.1 existe pour empêcher. Aucun pixel ne bouge |
| [0081](0081-grid-page-cost.md) | **La page de grille ne trie plus la bibliothèque entière** : `catalog.md` §35 promettait un défilement instantané sur des centaines de milliers d'assets et une absence de jointure superflue — mesuré sur 50 000 assets, ni l'un ni l'autre. Trois faits, dont deux invisibles à la lecture : SQLite **matérialise la ligne de sortie complète avant de trier**, donc les deux pastilles corrélées (« développée » d'[ADR 0055](0055-library-navigation.md) §5, `RAW+J` d'[ADR 0079](0079-raw-jpeg-pairing.md) §6) s'exécutaient 50 000 fois pour afficher 100 vignettes ; la taille de la fenêtre ne changeait presque rien (100 lignes ≈ 1 000 lignes), le coût étant le tri de tout l'ensemble filtré ; et le terme d'expression en tête du `ORDER BY` interdisait tout index — alors qu'il était **redondant** en descendant, SQLite y triant déjà les NULL en dernier. D'où une page en deux niveaux (CTE `page` sur deux entiers par ligne, décoration des seules survivantes), un `ORDER BY` sans expression (`NULLS LAST` en ascendant) et un index couvrant `assets(companion_of, capture_date, id)` — la clause que **toute** grille porte, puis le tri par défaut. **0,46 ms contre 10,32 ms** en tête (×22), ×3,6 sur le tri par date d'import, et l'ordre observable **identique** sur les quatre tris, vérifié ligne à ligne. `count()` garde sa forme plate, n'ayant ni tri ni colonne à y gagner. Renversement par la mesure, après écriture du code : `idx_assets_companion` devait être supprimé comme préfixe redondant — le garder rend `count()` **8× plus rapide** (3,3 contre 28,7 ms), parce que c'est lui que le comptage parcourt d'un bout à l'autre. Restent linéaires et assumés : la pagination par `OFFSET` (41 ms au fond de 50 000, contre 77 avant) et un filtre qui ne ramène rien. `Catalog::grid` garde son contrat, aucun pixel ne bouge, §5.1 hors de cause |
| [0082](0082-embedded-preview-at-import.md) | **L'import montre l'imagette du boîtier, il ne la rend pas** : mesuré, importer 10 CR2 prend 6,95 s dont **0,1 s d'import réel** — tout le reste est `generate_import_thumbnails`, qui rend une vignette de 256 px à travers un **décodage capteur** par fichier (~680 ms). Sur 15 000 CR2 : 2 h 50, dont cinq minutes d'import. La décision était déjà prise une étape plus tôt : [ADR 0065](0065-selective-import.md) §2 (« L'imagette vient du fichier, jamais du pipeline ») la justifie mot pour mot, et la seule raison qu'il donnait de ne pas garder ces imagettes — « le cache est indexé par asset, ces fichiers n'en ont pas » — disparaît une fois l'asset créé. Mesures sur 7 CR2, 2 boîtiers : l'imagette embarquée est un **JPEG pleine taille** (5184×3456, parfois **plus grande que ce que le RAW décode** en mRAW) ; l'extraire coûte 41 ms, la **décoder 76**, la réduire et l'écrire 5 — **122 ms contre 680, ×5,6**, soit **31 min contre 2 h 50** sur le corpus. La ladder complète (4 classes du même décodage) est mesurée **et rejetée** : 276 ms et 16,9 Mo par photo, 250 Go sur 15 000. Le point de conception est ailleurs : une imagette de boîtier **n'est pas le rendu d'une révision**, et l'y faire passer ferait mentir `valid_preview` pour toujours — aucun rendu ne viendrait la remplacer et un `undo` ressortirait le JPEG Canon en croyant montrer un développement. D'où une colonne `previews.origin` (migration 6), portée par la ligne et non déduite d'un chemin ; `valid_preview` gagne `AND origin = 0`, l'imagette s'ancre sur la révision initiale et échappe à la fenêtre de rétention d'[ADR 0075](0075-preview-cache-retention.md) — elle appartient au fichier, pas à l'historique. `cached_preview` rend **la meilleure image affichable et dit si elle est définitive** ; le minuteur de vignettes de Studio, qui vide déjà une file `missing`, ne change que de critère : **on paie le rendu de ce qu'on regarde, pas de ce qu'on importe**. `ImportOptions` hérite du `thumbnails: bool` de `ScanOptions` (~20 ms/fichier, ×35) — une option d'import, pas une préférence ([ADR 0078](0078-preferences-panel.md) §1). Le prix est dit : la grille montre le rendu **Canon** sur une photo jamais visitée, puis le rendu Leyline — **la couleur change une fois sous les yeux de l'utilisateur**, ce que Lightroom vend sous le nom « Embedded & Sidecar ». Une imagette absente, illisible ou **plus petite que la classe demandée** retombe sur le rendu : une vignette floue agrandie serait pire qu'une vignette lente. L'orientation était un piège déjà désamorcé par `scan.rs::embedded_preview` (LibRaw ne tourne pas l'imagette, qui porte *parfois* sa propre balise EXIF). Reste ouvert et nommé : le décodage JPEG **pleine résolution** (17,9 Mpx pour produire 256 px) devient 62 % de la passe, et `zune-jpeg` n'expose pas le décodage à l'échelle du DCT — décision de dépendance, non mesurée. Aucun pixel du pipeline ne bouge, §5.1 hors de cause. **Un compagnon RAW+JPEG n'a pas de vignette à produire** (§5) : [ADR 0079](0079-raw-jpeg-pairing.md) §5 le sort de la grille et le volet de détails n'en montre que le nom — aucun écran n'affiche sa vignette, et la passe en produisait une quand même, soit **la moitié du travail jetée** sur un dossier réglé ainsi. Neutre par construction : sans paires, rien n'est sauté. Sur 29 paires réelles du corpus, ~27,5 s → **~4,6 s** (×6, dont ×2 pour §5 seul). Et le compagnon **n'est pas une meilleure source** pour la vignette du maître, contre l'intuition : mesuré **161 ms contre 119**, parce que c'est un encodage de meilleure qualité (5,2–10,6 Mo contre 1,3–3,2) donc plus long à décoder, pour la même image. Dépairage et passe d'appairage explicite retombent sur le chemin paresseux.. **Révisé** après relecture : la décision porte sur le **chemin d'aperçu**, pas sur la passe d'import — la mettre dans la passe aurait laissé le chemin **paresseux** (celui qui remplit la grille pendant qu'on la parcourt) payer 680 ms par cellule visible, soit plus d'une minute par écran, faisant de la passe le seul moyen d'avoir une grille utilisable : l'inverse du but. D'où §4 : **l'import rend la main dès que le catalogue est écrit** (~20 ms/fichier) et la passe part en travail de fond — remplir le catalogue et remplir le cache sont deux travaux. Et elle est **parallèle** : le commentaire du code donnait une raison exacte de ne pas la paralléliser (sérialisation sur les mutex du cache de décodage et du cache d'étages, le verrou catalogue étant déjà relâché par [ADR 0023](0023-catalog-lock-narrowing-preview.md)), et une vignette tirée de l'imagette n'en touche aucun — l'objection tombe avec sa cause. **128 → 24 ms/fichier, ×5,3 mesuré** sur 16 cœurs. Sur 15 000 CR2 : catalogue utilisable en **~5 min** au lieu de 2 h 50, grille entièrement chaude ~6 min plus tard en fond, et **122 ms par cellule** pour qui ne préchauffe jamais.. La passe **suit l'ordre de la grille**, pas celui de l'import (demander les premières lignes de la requête par défaut coûte 0,46 ms depuis [ADR 0081](0081-grid-page-cost.md) — l'ordre est gratuit) ; mais **la première page n'est déjà pas le problème** : `AssetsAdded` déclenche `reload` → `load_window`, qui partitionne les vignettes manquantes **lignes visibles d'abord**, et Studio connaît son filtre et son tri là où le moteur ne les connaît pas. Un écran de 100 cellules se remplit en **~4 s** contre ~23 aujourd'hui.. **Implémentée** les 2026-08-26/27 (migrations 6, `previews.origin`). Un arbitrage a été tranché en écrivant le code, §1 et §3 se contredisant : l'imagette **est** la vignette tant que la photo n'est pas développée — elle n'est pas un bouche-trou remplacé dès qu'on regarde la cellule. La branche écartée coûtait 680 ms de processeur par photo parcourue, à vie, et faisait changer la couleur de chaque vignette sous les yeux de l'utilisateur ; celle-ci ne la fait changer qu'à la **première retouche**, quand il s'y attend. Conséquence heureuse : **Studio ne change pas d'une ligne**, un client n'ayant besoin d'aucune notion de provenance. Mesuré sur le binaire réel, 10 CR2, cache chaud : **13 ms/fichier** sans chauffe (contre 695) et **33 ms/fichier** chauffe parallèle comprise, à 500–640 % de CPU — soit ~3 min et ~8 min sur 15 000 CR2, contre 2 h 50. |
