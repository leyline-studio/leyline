CLAUDE.md
/target
**/target
.idea/
# CI workflow kept local until the gh token gets the workflow scope
.github/
# cross-build artifact (mingw-built libraw_r DLL), regenerated per-machine
/packaging/windows/vendor/
